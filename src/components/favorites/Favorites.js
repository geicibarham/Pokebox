import styles from './favorites.module.css'

const Favorites = () => {
    return (
        <section className={styles.favorites}>
            <h4>Favorites</h4>
        </section>
    )
}

export default Favorites